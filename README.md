My personal test of gtkmm GUI for linux (actually Arch linux is used).

# COMPILE

For compile put the compile.sh shell script inside the same c++ program directory and edit the name of file.
Than use sudo chmod +x compile.sh and execute it "./compile.sh"
